// src/core/config.js

// URL dinámica del backend. 
// Si estamos en el ordenador, usa localhost. 
// Si estamos en el móvil, usa la IP del ordenador automáticamente.
export const API_BASE = "/api";

console.log(`🔌 [CONFIG] API Base set to: ${API_BASE}`);
